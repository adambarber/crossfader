#!/usr/bin/env ruby

Signal.trap('INT') { exit 1 }

require 'crossfader'

def pute(*args)
  first = args.shift.dup
  first.insert(0, "#{$PROGRAM_NAME}: ")
  args.unshift(first)
  $stderr.puts(*args)
end

begin
  Crossfader::CLI.start(ARGV)
rescue Interrupt
  pute 'Quitting...'
  exit 1
end